<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base theme: Compose draws the real Material 3 UI, so we only need a
         no-action-bar host theme with the brand splash background color. -->
    <style name="Theme.PB123Hub" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/pb_blue</item>
        <item name="android:statusBarColor">@color/pb_blue</item>
    </style>
</resources>
